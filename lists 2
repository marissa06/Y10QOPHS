# challenge 2 - lists

cars = []

end = True
while end == True:
    choice = input("enter a car brand or press x to exit>>>")
    if choice == "x":
        end = False
    else:
        cars.append(choice)
        print(cars)

